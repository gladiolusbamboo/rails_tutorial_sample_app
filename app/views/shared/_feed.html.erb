<% if @feed_items.any? %>
  <ol class="microposts">
    <%# @feed_itemsはMicropostのリストなので %>
    <%# 自動的にMicropost表示のパーシャル %>
    <%# /sample_app/app/views/microposts/_micropost.html.erb %>
    <%# が参照される %>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>